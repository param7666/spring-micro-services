spring.application.name=BootMSProj07-BillingService-ProviderMS

spring.appication.instanceId=${spring.application.name}:${random.value}

server.port=8084

eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# make  this  MS (Config Client) connecting to Config Server  
spring.config.import=optional:configserver:

#Activate the actuators
#management.endpoints.web.exposure.include=*